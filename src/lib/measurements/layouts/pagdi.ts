import type { MeasurementLayoutRow } from "../types"

export const pagdiLayout: MeasurementLayoutRow[] = [
  [
    {
      "label": "Pagdi Forhead Size",
      "name": "pagdi_forhead_size"
    }
  ]
]
